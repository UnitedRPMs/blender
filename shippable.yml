# Build Environment

language: python

python:
  - 3.0

install: true

script:
- docker run --privileged=true --cap-add=SYS_ADMIN  -v $(pwd)/34:/root/enjoy/ --security-opt seccomp:unconfined davidva/unibuild-34 /bin/sh -c "rm -f /urpms && wget -c https://raw.githubusercontent.com/kuboosoft/united-build/master/urpms && chmod a+x urpms && /urpms -g UnitedRPMs/blender -s blender.spec -r true -d 'dist .fc34' -u true"   

notifications:
slack: unitedrpms:lzDykHTpajKrgmGz9NkrO1Hj





